This is a Docker test. It implements Nginx, Php-fpm and Mariadb and a test environment is created.
1. Clone the repository
2. Execute the following command from the directory where docker-compose.yml resides

sudo docker-compose up

This will start Nginx, php-fpm, mariadb containers
